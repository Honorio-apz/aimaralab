---
const projects = [
  {
    title: "AiMara Chatbots",
    description: "Un proyecto centrado en la creación de corpus de datos específicos según la necesidad y creación de chatbots con modelos de inteligencia artificial para optimizar los procesos administrativos en instituciones públicas y privadas.",
    image: "/chatbot.png"
  },
  {
    title: "AiMara Translator",
    description: "Proyecto enfocado en la generación de corpus de datos en lenguas nativas peruanas, como quechua y aimara, para el desarrollo de modelos de inteligencia artificial capaces de traducir, reconocer audios y generar contenido contextual y audio en estos idiomas.",
    image: "/translator.jpeg"
  },
  {
    title: "AiMara Legal",
    description: "Proyecto destinado a desarrollar un modelo de inteligencia artificial que comprenda completamente el marco legal peruano, facilitando el acceso a la justicia de manera equitativa y sin discriminación.",
    image: "/nlplegal.jpeg"
  }
];
---

<section id="proyectos" class="bg-gray-100 w-full mt-12">
  <div class="mx-auto w-full lg:px-24 max-w-7xl md:px-12 items-center px-8 py-12 scroll-mt-12 bg-gray-100">
    <div class="mx-auto text-center max-w-3xl">
      <h1 class="text-4xl text-center tracking-tight font-medium text-black md:text-5xl font-display mb-4">
        Proyectos
      </h1>
      <div class="h-1 w-24 bg-celeste_aimara mx-auto mb-6"></div>
      <p class="text-lg text-gray-600 mb-12">
        Los proyectos de AiMara se desarrollan basados en necesidades o problemas detectados de la sociedad en general, y brindan una solución efectiva.
      </p>
    </div>
    
    <div class="grid grid-cols-1 gap-10 mt-10 lg:grid-cols-3">
      {projects.map((project) => (
        <div class="bg-white rounded-lg shadow-md transition-all duration-300 hover:shadow-xl flex flex-col hover:-translate-y-2">
          <img src={project.image} alt={project.title} class="w-full h-48 object-cover rounded-t-lg" />
          <div class="flex-1 p-6">
            <h3 class="text-xl font-bold text-gray-800 mb-3">{project.title}</h3>
            <p class="text-gray-600">
              {project.description}
            </p>
          </div>
        </div>
      ))}
    </div>
    
    <div class="flex justify-center mt-12">
      <a
        href="/proyectos"
        class="inline-flex items-center justify-center px-6 py-3 text-base font-medium text-white bg-gray-800 rounded-lg group focus:outline-none focus-visible:outline-2 focus-visible:outline-offset-2 hover:bg-boton_aimara hover:text-black transition-colors duration-300 ease-in-out"
      >
        Ver Todos
      </a>
    </div>
  </div>
</section>

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  #proyectos .grid > div {
    animation: fadeInUp 0.6s ease-out forwards;
    opacity: 0;
  }

  #proyectos .grid > div:nth-child(1) {
    animation-delay: 0.2s;
  }

  #proyectos .grid > div:nth-child(2) {
    animation-delay: 0.4s;
  }

  #proyectos .grid > div:nth-child(3) {
    animation-delay: 0.6s;
  }
</style>