---
import events from './events.json';
---

<head>
  <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@200;400&family=Roboto:wght@100&display=swap" rel="stylesheet">
</head>

<section class="bg-gradient-to-br from-gray-50 to-gray-100 py-16">
  <div class="container mx-auto px-4 max-w-7xl">
    <div class="flex flex-col lg:flex-row items-start">
      <div class="lg:w-1/4 lg:sticky lg:top-8 self-start mb-12 lg:mb-0 pr-8">
        <h2 class="text-3xl font-roboto tracking-tight text-gray-900 sm:text-3xl">Our events</h2>
        <p class="mt-4 text-lg text-na mx-auto text-celeste_aimara">
          _____________
        </p>
        <br>
        <p class="text-base text-gray-600 mb-8">
          We are pleased to present a series of events designed to inspire, educate, and connect the community. At each event, distinguished professionals and experts will share their knowledge and experiences, addressing the latest trends and advancements in technology, science, and entrepreneurship.
        </p>
      </div>
      <div class="lg:w-3/4 lg:pl-12">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          {events.map(event => (
            <div class="bg-white rounded-lg shadow-lg overflow-hidden transform transition duration-300 hover:shadow-xl hover:-translate-y-2 float-animation">
              <div class="relative overflow-hidden cursor-pointer" onclick={() => window.location.href = event.link}>
                <img src={event.image} alt={event.title} class="w-full h-48 object-cover transition duration-300 hover:scale-110" />
                <div class="absolute inset-0 bg-black bg-opacity-50 opacity-0 hover:opacity-100 transition duration-300 flex items-center justify-center">
                  <span class="text-celeste_aimara text-white text-lg font-semibold">Ver detalles</span>
                </div>
                <div class="absolute top-0 left-0 bg-black text-white px-3 py-1 text-sm font-semibold">
                  {event.date}
                </div>
              </div>
              <div class="p-6">
                <h3 class="text-xl font-semibold text-gray-800 mb-2">{event.title}</h3>
                <p class="text-sm text-celeste_aimara mb-2">{event.time}</p>
                <p class="text-sm text-gray-500 mb-4">{event.location}</p>
                <a href={event.link} class="rounded-lg bg-gray-700 border-gray-800 px-3 py-2 text-sm font-medium text-white hover:bg-black hover:bg-celeste_aimara/90 transition-colors duration-150">
                  Ver detalles
                </a>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
  .float-animation {
    animation: float 6s ease-in-out infinite;
  }
  .float-animation:nth-child(2n) {
    animation-delay: -3s;
  }
</style>
